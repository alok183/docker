# docker

### start salt-master & salt-minion as a container
 - [Salt](salt)\
 cd salt/\
 docker-compose up -d
### start ansible server as a container
 - [Ansible](ansible)\
 cd ansile/\
 docker-compose up -d
